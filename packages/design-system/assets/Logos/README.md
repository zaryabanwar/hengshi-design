# Logos

Framework Relay, the current exploratory logo candidate: EXPLORATORY, UNREGISTERED, TRADEMARK NOT CLEARED. Each file carries its own field colour and strokes, so show it with `<img>` and never recolour it: `logo-framework-relay-primary.svg` (paper brackets, `#00D4FF` relay on `#0B0F14`) on ink; `logo-framework-relay-light.svg` (ink brackets, `#006D82` relay on `#FAF9F6`) on paper or white; `logo-framework-relay-mono.svg` one black ink on white; `logo-framework-relay-reverse.svg` one white ink on black; `mark-framework-relay.svg` at 32 CSS px and above; `mark-framework-relay-small.svg` at 24 to 31 CSS px; `favicon-framework-relay.svg` at 16 to 23 CSS px. Horizontal lockups are never narrower than 168 CSS px; clearspace is 20 master units on every side. Full name first: the mark never stands for the entity alone. Construction, sizes and prohibitions are in the Logo system section.
