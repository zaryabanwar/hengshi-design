# Lockup

The Framework Relay logo as shipped, placed at the sizes the geometry allows, with the full name first. Built from `docs/phase-1-brand-identity/geometry-spec.json` and the SVGs in `assets/Logos`; covers PRIM-062 Logo and entity lockup.

**When to use.** The site header and footer, documents and social images, after the full name "Hengshi Design" has been established in text. The mark alone never defines the entity.

**Provide.** An `<img>` of the right asset for the field and size: `logo-framework-relay-light.svg` on paper or white and `logo-framework-relay-primary.svg` on ink for the horizontal lockup (never narrower than 168 CSS px); `mark-framework-relay.svg` at 32 CSS px and above, `mark-framework-relay-small.svg` at 24 to 31, `favicon-framework-relay.svg` at 16 to 23; mono or reverse for one-ink contexts. Alt text "Hengshi Design" on the lockup; empty alt on a mark beside the visible name.

**Anatomy.** `.hd-lockup` (min-width 168px) with `.hd-lockup--mark`, `--mark-small` or `--favicon` fixing the mark widths. Each SVG carries its own field colour, and clearspace applies outside it: 20 master units on every side.

**Do.** Show the lockup on a flat approved field; keep the retained strokes at 2 device pixels or more; test on DPR 1.

**Don't.** Recolour, stretch, round, enclose or animate it; repeat its brackets as a UI frame; use it in structured data, filings or production before the trademark and founder gates close. Every file is EXPLORATORY, UNREGISTERED, TRADEMARK NOT CLEARED.
