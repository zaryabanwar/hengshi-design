# Hengshi Design Risk Register

**Classification:** Business confidential
**Last reconciled:** 2026-09-06
**Risk acceptance authority:** Founder; no risk is accepted unless explicitly
recorded as a decision.

Likelihood and impact are planning assessments, not measured probabilities. Named
people are deliberately not assigned until the founder supplies or approves them.

| ID | Risk | Likelihood | Impact | Status | Evidence / trigger | Treatment and exit evidence |
|---|---|---:|---:|---|---|---|
| R-001 | Legacy v3 product documents, active technical references, backlog, diagrams, and draft specs still contain fixed React 18/PostgreSQL 16 or AWS-era intent even though Phase 0 governance now requires latest-compatible stable versions and Azure. | Certain | High | in_progress | Constitution 2.0.0, amended `AGENTS.md`/software definitions, D-004/D-014, legacy artifacts | Phase 1 supersedes or reconciles the legacy product/feature set and obtains founder approval before modernization. The governance-level conflict is resolved. |
| R-002 | The working tree contains substantial user-owned changes, so unrelated edits or cleanup could destroy provenance. | Certain | High | in_progress | Phase 0 baseline: 16 modified tracked, 63 untracked, 0 staged | Preserve all pre-existing work; use bounded non-overlapping scopes; inspect every diff; no commit/revert/move without its gate. |
| R-003 | `WorldHUD` conditionally returns before `useMemo`, which can cause a deterministic React hook-order failure. | High | High | blocked | `apps/web/src/world/WorldHUD.tsx` | Phase 2 adds a failing regression test, fixes hook order, and verifies entry/inside transitions in Playwright. |
| R-004 | `require_admin` does not enforce the admin role, so authenticated non-admin users can satisfy the dependency. | High | Critical | blocked | `apps/api/app/auth/deps.py` | Phase 2 adds negative authorization tests, enforces role mapping, and obtains independent security review. |
| R-005 | Missing `DATABASE_URL` silently falls back to SQLite, conflicting with PostgreSQL-only application runtime and potentially hiding migration/data defects. | High | Critical | blocked | `apps/api/app/db/session.py` | Phase 2 makes application startup reject missing/invalid PostgreSQL configuration; SQLite remains explicit test-only; migration/restore evidence passes on PostgreSQL. |
| R-006 | JavaScript ranges and materially unpinned Python requirements make the observed dependency graph non-reproducible and increase supply-chain drift. | High | High | blocked | `apps/web/package.json`, `apps/api/requirements.txt` | Phase 1 approves a compatibility matrix and locking approach; Phase 2 upgrades in isolated waves with SBOM/license and rollback evidence. |
| R-007 | Product, SEO, UX, architecture, AI, publication, security/privacy, and launch definitions are not yet coherent enough to authorize application development. | Certain | Critical | blocked | Existing v3 documents and feature placeholders conflict with D-001–D-023 | Complete and approve Phase 1; trace every decision to specification and acceptance evidence. |
| R-008 | Existing seeded projects, outcomes, expert identities, and company/legal details may be fictional, incomplete, or unverifiable. | High | Critical | blocked | Current prototype content; founder-approved proof policy | Build a claim/proof inventory; quarantine unverifiable material; require specialist evidence review and founder/legal approval. |
| R-009 | The protected 10,002,012-byte GLB lacks a project-owned editable source and its exact original license/provenance is not recorded. | High | High | blocked | `apps/web/public/models/hengshi-hq-atlanta-exterior-web.glb`; no project `.blend`/FBX/GLTF source found | Obtain source/license evidence before geometry reuse; otherwise use it only as a visual reference and rebuild original licensed geometry. |
| R-010 | Hengshi Design has a greenfield SEO/entity footprint and can be confused with unrelated “Hengshi” organizations. | High | High | blocked | No approved canonical semantic route/publication system; entity-name collision identified in planning | Phase 1 approves entity/keyword/route/content strategy; Phase 3 implements atomic prerendering/index controls; monitor qualified organic outcomes. |
| R-011 | A cinematic 3D experience can miss mobile, accessibility, Core Web Vitals, load, memory, or recovery targets. | High | High | blocked | Current single GLB already exceeds the approved mobile initial transfer budget | Design for adaptive streaming, LODs, semantic parity, reduced motion, non-WebGL recovery, and enforce budgets in Phase 5 acceptance tests. |
| R-012 | Azure, Microsoft Graph/Teams/Outlook, NVIDIA, DNS, and search integrations may lack account access, regional GA capacity, licensing, consent, or approved cost. | Medium | Critical | blocked | Manual actions MA-005 through MA-010 | Capability-test safely, select current GA services/regions, approve cost envelopes, and record authenticated operation separately from configuration. |
| R-013 | There is no repository GitHub Actions workflow, immutable artifact evidence, deployment environment, monitoring, or rollback drill. | Certain | Critical | blocked | `.github/workflows` absent at Phase 0 observation | Define in Phase 1, implement after gates, then verify candidate deployment, observability, traffic switch, and rollback before launch. |
| R-014 | The initial RPO <=24h and RTO <=8h may become inadequate once retained leads/bookings are commercially material. | Medium | High | in_progress | D-020 | Keep as an explicit launch risk; test restoration; reassess when retained lead/booking value or contractual needs increase. |
| R-015 | Historical February 2026 readiness percentages may be mistaken for present local, UAT, or launch acceptance. | High | High | in_progress | `docs/Hengshi_Design_Project_Consolidation_v3.docx` | Use `PROJECT_STATE.yaml` readiness only; require deterministic evidence for every later readiness claim. |
| R-016 | Defense-facing content or AI could invite classified, controlled, export-sensitive, or unsupported claims/data. | Medium | Critical | blocked | D-005, D-017; legal/security evidence unavailable | Keep defense unpublished/unindexed until independent legal/security/claim review and approved secure human routing are complete. |
| R-017 | Publication/AI index divergence could expose drafts, rejected claims, or stale content. | Medium | Critical | blocked | Atomic publication system does not yet exist | Phase 1 locks release contracts; Phase 3 uses one approved release checksum, atomic render/index, draft isolation, and rollback tests. |
| R-018 | AI failover can route confidential data to a provider that lacks approved residency, DPA, or production entitlement. | Medium | Critical | blocked | Dual-provider architecture approved but provider contracts not yet verified | Default confidential/PII traffic to approved Azure route; NVIDIA public/de-identified only until licensing, DPA, residency, and security gates pass. |
| R-019 | Local DOCX visual QA can be misreported when the installed converter is assumed operational without a real conversion and page-render check. | Low | Medium | closed | LibreOffice 26.2.4.2 passed a generated control DOCX conversion and the 24-page historical consolidation conversion on 2026-07-19; all target pages rendered and were inspected; independent QA returned pass. See `docs/phase-1-foundation/evidence/libreoffice-capability-2026-07-19.md`. | The repaired incident is closed. Residual control: every material DOCX visual gate performs a fresh harmless control conversion plus full rendered-page inspection. This closure does not approve the historical source document's content or layout. |
| R-020 | The identity data specimen's direct labels, series grammar, semantics, text spacing, and forced-colors evidence required independent closure before identity approval. | Low | Medium | closed | D-034 clean browser report `6BCB2C…D8A4`; independent design/accessibility PASS; D-035 identity acceptance | Keep later production UI and real assistive-technology testing separate; current closure is identity-definition evidence only. |
| R-021 | The D-032 browser pass exposed `.dependency-key` incomplete results and a print three-node contrast violation. | Low | High | closed | D-034 browser 0 violation/0 incomplete; independent reports `45CD6B…255C` and `B37412…FA54`; D-035 acceptance | Preserve the frozen source/evidence and re-test any later production implementation independently. |
| R-022 | Malformed validator selectors could prevent identity evidence from reaching assertions. | Low | High | closed | D-031 zero malformed hits; D-032 validator 59 PASS / 0 FAIL | Keep selector syntax covered by the deterministic validator. No broader harness refactor is authorized in this identity gate. |
| R-023 | Validator assertion literals can lag durable authority wording or pinned artifact metadata even when the underlying authority, hash, and inventory are correct. | Low | High | closed | D-032 corrected exactly two audited literals; validator 59 PASS / 0 FAIL | Prefer canonical IDs or structured values over prose in later validator maintenance. The current identity sources remain frozen. |
| R-024 | The identity validator conflated pre-change and post-change state after authorized changes. | Low | High | closed | D-034 explicit PRE/AUTHORIZED_POST/FINAL_FREEZE states; final validator 63/63; D-035 accepted the frozen package | Retain explicit lifecycle-state modeling in future validators; never assert authorized mutable inputs remain immutable. |
| R-025 | Approving MA-025 could be mistaken for enabling production, because no Figma or Stitch MCP is configured or exposed on this host and the canonical inventory `%APPDATA%\Code\User\mcp.json` named by `AGENTS.md` does not exist. **Closed 2026-09-06:** the stated exit evidence now exists. | High | High | closed | 2026-09-06 inventory check across six candidate locations; the now-superseded claims in `docs/software-definition/02-ai-dev-tooling-and-mcp.md` | `docs/phase-1-ui-reference-production/UI_REFERENCE_PRODUCTION_CONTRACT.md` §3.5 requires enablement Path A or Path B to complete and be recorded before the first external write, and the MA-025 gate text states the blocker before the founder decides. The stale tooling rows have been corrected. Exit evidence: a dated `docs/phase-1-ui-reference-production/validation/capability-evidence.md`. **Delivered 2026-09-06** — Figma is registered at user scope, authenticated by the founder, bound into a restarted session, and capability-tested by a read-only rate-limit-exempt `whoami`. The gate text stated the blocker before the founder decided, so approval was informed rather than mistaken. |
| R-026 | Unverified Figma free-tier limits (file count, pages per file, version-history retention, editor seats, Dev Mode, MCP tool-call allowance) could make the contracted evidence volume infeasible after production has already started. **Partly realized 2026-09-06, before production started.** | Certain | High | realized_and_treated | The 2026-09-06 pricing-page fetch rendered its comparison grid without attributable plan columns, so no limit could be sourced | Do not assert the limits. Contract §7.2 records all five as `[MUST VERIFY AT GATE]` and makes verification a precondition of the first write. B01 is a gated pilot so real evidence volume is measured on the cheapest batch. If a verified limit blocks contracted evidence, stop condition `SC-05` halts work and routes to MA-010 rather than triggering a purchase. Because retention may be time-limited, §9 makes the repository export — not provider version history — the authoritative rollback artifact. **Outcome 2026-09-06:** the control worked as designed. The sixth item, the MCP tool-call allowance, verified at **20 calls per calendar month** for this Full seat on the `starter` tier and immediately blocked the contracted volume — a 21-call floor across seven batches before any visual evidence. `SC-05` fired at programme scope *before* the first write rather than mid-production, which is exactly what refusing to assert the limit bought. The founder was shown the shortfall and directed remaining on the free tier with precise rationing, so the risk is treated by `mcp-call-budget.csv` `CB-01`–`CB-15` and counted by `mcp-call-ledger.csv`, with B01 measuring true cost. **Four of the six items remain unverified and unasserted**, so this row stays open; version-history retention is off the critical path because §9.4 already makes the repository export authoritative. |
| R-027 | MA-025 could be read as approving the whole nine-batch reference programme, when B07 and B09 are not authorizable and the D-037 §15 handoff therefore cannot complete under it. | Medium | High | in_progress | `design-batch-plan.csv` B07 prerequisites against `MA-004: blocked` and `three_d_storyboard_and_asset_specification: not_started`; B09 hard-requires B07 | The split is stated in the MA-025 gate text, the package README, contract §2.1, `batch-production-plan.csv`, and trace rows TR-005–TR-007, and is enforced by validator authorization assertions and stop condition `SC-10`. Exit evidence: an approved MA-026 covering B07, B09, `docs/ui/UI_SPEC.md`, and the final visual direction. |

| R-028 | The HQ exterior model's source page carries a "NoAI" notice forbidding use "as inputs to generative AI programs", while the planned surroundings method is Blender driven by an AI agent. Under CR-002 this would block a required stream, not an optional one. | Medium | High | awaiting_human | `docs/phase-1-3d/asset-provenance/HSD-ASSET-001-hq-exterior.md` §7; notice quoted verbatim from the 2026-09-06 source-page fetch | Do not resolve by engineering judgement. Three dispositions are recorded for founder decision: adopt the narrow reading and record the reasoning; keep the mesh human-edited and have the agent author scripts without ingesting it; or replace the asset. No download, import, or Blender session has occurred. Note the unresolved tension that CC BY 4.0's "no additional restrictions" term may make the notice unenforceable as a licence condition. |
| R-029 | CC BY 4.0 attribution is mandatory for the HQ exterior model, but none of the 33 accepted routes is a credits or attribution surface, so a live site could ship in breach of the licence. | Medium | Medium | in_progress | `HSD-ASSET-001` §5 obligations ATTR-01/02/03; D-025 route inventory contains no credits route; the file's `asset.copyright` field is absent | Treat attribution placement as a real UI decision inside CR-002 rather than a footnote, covering every stream that renders the model including a still image in the no-WebGL stream. Also record the modification statement, since the file is already a Blender re-export and is therefore a derivative today. Exit evidence: an approved attribution surface plus an embedded or in-repository copyright notice. |
| R-030 | The CR-002 stream-selection rule depends on reading device-capability and connection signals in the browser, and that support is believed to be uneven across engines. The four-tier model could prove unimplementable as specified after it is approved. **Verified 2026-09-06: the belief was correct, and the rule as written is not implementable.** | Certain | High | realized_and_treated | CR-002 §7 uncertainties U-01 and U-02, both recorded as `[MUST VERIFY AT SPECIFICATION]` rather than asserted | Do not assert the support matrix. Verify against Context7 and official primary documentation before any schedule is offered. Design the rule to degrade safely: when a signal is missing, select the stream most likely to succeed rather than the most impressive, and surface the user override. If portable selection proves unachievable, stop and return CR-002 Option B (two streams) to the founder instead of inventing an unsupported mechanism. **Outcome 2026-09-06, recorded in `CR-002-signal-portability-verification.md`:** the support matrix was verified rather than asserted, and it split. Connection signals are not portable at all — the Network Information API and `prefers-reduced-data` are both outside Baseline, the latter warned as supported by no user agent. `deviceMemory` is likewise outside Baseline and coarsened to power-of-two buckets. But `hardwareConcurrency` is Baseline widely available since March 2022 and `prefers-reduced-motion` since January 2020, so capability tiering and accessibility precedence stand on firm ground. **The stop was therefore not triggered**, because selection is achievable once connection class is replaced by measured runtime performance — the substitution CR-002 §7 named in advance as its fallback, which keeps this inside the approved change rather than an invented mechanism. Residual: measured-performance selection is itself unproven and is **not asserted**; if it fails, the stop condition still applies and Option B returns to the founder. The missing-signal path is now the default for Firefox and Safari visitors rather than an edge case, so the conservative default must be designed for, not handled as an exception. |

| R-031 | Figma's canvas-write capability — the exact capability every MA-025 batch depends on — is free only during beta and is documented to become "a usage-based paid feature". **Re-scoped 2026-09-06:** the residual exposure is loss of capability, not unexpected spend. | Medium | Medium | accepted_by_founder | Figma's Dev Mode MCP guide, fetched 2026-09-06: canvas writing "requires the remote Figma MCP server", and the canvas-writing and UI-capture features "will eventually be a usage-based paid feature" | **No recurring pricing check.** The founder directed on 2026-09-06 that the account is on a free plan and that no such check should be performed. That reasoning holds for the spend half of the risk: a free plan with no payment method on file cannot be charged silently, so the zero-spend boundary is protected by the account itself rather than by a check. The half it does not cover is availability — if canvas writing moves behind a paywall, batches stop working. That needs no check because it announces itself: the write simply fails or prompts to upgrade. Treatment is therefore reactive, not scheduled — stop condition SC-04 halts on any paid prompt, and a blocked write halts the batch and routes to MA-010. No purchase may be made to unblock it. |
| R-032 | Founder-directed additions can silently bypass reconciliation: `/credits` is a 34th route against D-025's accepted 33, and the route inventory lives inside the frozen D-037 package. | Medium | Medium | closed | `HSD-ASSET-001` §6a decision D-B; `foundation-route-coverage.csv` enumerated 33 routes at the time this row was written and enumerates **34** since the CR-002 reconciliation | Authorized by founder direction, but explicitly recorded as unreconciled in `HSD-ASSET-001`, MA-004, and this register rather than treated as done. Reconciliation into the route inventory, UX wayfinding, route-coverage CSV, and the footer primitive is assigned to the CR-002 revision window instead of a third concurrent change request. `/credits` must appear in every delivery stream because the licence obligation does not vary by tier. **Closed 2026-09-06** by the CR-002 revision window: `ROUTE-CREDITS` `/credits` added to `06-canonical-route-inventory.json` (D-025 package), `route-room-parity.csv` (D-036), and `foundation-route-coverage.csv` plus `traceability.csv` `TR-ROUTE-034` and B02 primary ownership (D-037); the `PRIM-001` shell anatomy now requires an attribution notice link; and style guide §8.2 states `/credits` appears in all four streams. Route counts moved 33 to 34 in the foundation, UX, and design validators, which now pass at RESULT=PASS with 34 routes. Residual: the amendment is unreviewed and unaccepted, tracked at MA-028, not here. |
| R-033 | The verified 20-call monthly Figma MCP allowance stretches the seven-batch programme across an unknown number of calendar months, and a month can be exhausted mid-batch, leaving a batch with a `_PRE` checkpoint and no `_EXIT` archive. | Certain | High | accepted_by_founder | `capability-evidence.md` §4; the 21-call structural floor against a 20-call month; founder direction 2026-09-06 "stay on course we will use 20 calls per month very precisely" | Accepted knowingly after the exact shortfall was reported, so this is a recorded constraint rather than a surprise. Treated by `mcp-call-budget.csv`: every call assumed metered (`CB-02`) because write-tool exemption is undocumented in scope; every call counted before the next is made (`CB-04`); coarsest permissible read granularity (`CB-13`); failed calls counted as consumed (`CB-14`); exhaustion halts and reports rather than being routed around (`CB-15`). **No schedule may be stated until B01 reports a measured per-batch projection (`CB-09`).** The mid-batch exhaustion behaviour is an open founder decision at `CB-07` and must not be given a producer default. Allowance may never be restored by purchase, second account, second seat, or trial (`CB-11`, `CB-12`). |
| R-034 | D-039 approved four peer delivery streams, but the accepted D-036/D-037 artifacts still describe 3D as an optional enhancement, so two contradictory accepted authorities coexist until the CR-002 revision window completes. | High | Medium | reopened | D-039 against `design-batch-plan.csv` B07 `batch_name` "Optional World HUD and Quick Access parity", `3D_Mega_Menu_Style_Guide_v2.md` §8.2 degradation framing, and SRS `FR-3D-010` "WebGL fallback" | Do not silently prefer either source. Until the revision window completes, D-039 is the governing statement of intent and the older artifacts are superseded-but-unedited, which is recorded here, in MA-027, and in the decisions log rather than left implicit. The window must amend all four artifacts in one bounded slice and reconcile `/credits` per R-032. Nothing may be implemented from either version while application work remains blocked, which limits the blast radius to documentation. Exit evidence: the amended artifacts plus a validator run that asserts no remaining "optional enhancement" or "fallback" framing for the semantic stream. **Closed 2026-09-06** with that exact evidence. Style guide §8.2 is now "Delivery Streams" with four peers and a founder-decided selection precedence; SRS `FR-3D-010` is the `S-SEMANTIC` peer stream at P0 with new `FR-3D-011` and `FR-3D-012`; `design-batch-plan.csv` B07 is "Core delivery-stream World HUD and semantic peer parity"; 15 residual degradation framings were removed across seven package artifacts; and the column `optional_immersive_representation` was renamed `immersive_stream_representation` in both CSVs and both validators. A new standing assertion `semantic-stream-peer-framing` fails the design validator if optional/fallback framing for the semantic stream is reintroduced. All four validators pass. Residual: the amended package is unreviewed and unaccepted, tracked at MA-028. **Reopened 2026-09-06 (D-042).** The independent accessibility audit found the rename was partly lexical: `FLOW-WORLD-HUD-FALLBACK`, the `WORLD_HUD_FALLBACK` journey key, and `MODE-NON-WEBGL-QUICK-ACCESS` still frame the semantic peer as a failure surface, and the `semantic-stream-peer-framing` guard is a four-alternative regex scoped to the design folder that cannot see any of them. These are accepted upstream identifiers whose rename cascades into the foundation and UX packages, so the rename is not attempted inside the CR-002 window. **Exit evidence:** the three identifiers renamed to peer vocabulary across foundation, UX, and design; the framing guard widened beyond its current regex and beyond the design folder; `BP-NFR-006` states which stream a below-floor client receives. **Closed 2026-09-06 (D-043), with a correction to the reopening note above.** The claim that the rename cascades into the foundation and UX packages was the producer's and it was **false**: verification found all 47 occurrences confined to the design package and to history documents that are deliberately not rewritten. There was no cascade and no reason to defer. `FLOW-WORLD-HUD-FALLBACK` → `FLOW-WORLD-HUD-STREAM`, `WORLD_HUD_FALLBACK` → `WORLD_HUD_STREAM`, `MODE-NON-WEBGL-QUICK-ACCESS` → `MODE-SEMANTIC-SHELL`, and `static_directory_fallback` → `static_directory_index`. The `semantic-stream-peer-framing` guard was widened to catch `*_fallback` identifier forms, `FLOW-*-FALLBACK`, `MODE-NON-WEBGL*`, quality downgrade, toggle quality, and quality tier. It was then deliberately **narrowed** to stream contexts, because a blanket `*_fallback` pattern also matched `booking_fallback` and `durable_fallback` — a provider contingency and a storage contingency that have nothing to do with the semantic stream; renaming those would have been collateral damage rather than peer framing, and the reason is recorded in the validator source beside the pattern. Design validator PASS 189/0. **Reopened 2026-09-06 — this closure was premature, for the second time on the identical defect class.** Both independent re-reviewers found surviving instances the producer's verification and the widened guard both missed, because both were scoped to the design package and to hyphenated identifier forms: the exact string this risk is recorded closed against survives in the **production** package's `batch-production-plan.csv` B07 ("Optional World HUD…"), and `MODE_NON_WEBGL_QUICK_ACCESS` survives in `TPL-QUICK-ACCESS-RECOVERY`'s `baseline_frame_name`, where **underscores** evade a guard written for hyphens. The producer has now closed R-034 prematurely twice; a third closure requires independent verification rather than a producer sweep. **Exit evidence:** an independent reviewer confirms no degradation or ladder framing for the semantic stream remains in the design package, the UX package, **or the production package**, in any identifier form including underscore-separated frame names; and the framing guard runs across all three packages with a pattern that covers both separators. **Updated 2026-09-06 (D-045) — a third producer sweep ran and this risk still does not close.** Both named survivors are gone: `MODE_NON_WEBGL_QUICK_ACCESS` was removed when all 74 `baseline_frame_name` values were regenerated under the amended `EC-08` grammar, and `batch-production-plan.csv` B07 no longer carries the optional framing. The guard now runs as `A-16` across all three packages in both separator forms, with a closed structural exemption for dated `reviews/` and `accessibility/` records and a strict-subset assertion that fails if the exempt set grows. It immediately found **six further files nobody had named** — `UX_ARCHITECTURE.md`, `STATES_AND_RECOVERY.md`, `FLOWS.md`, `README.md`, `excluded-surfaces.csv`, and `CONTENT_ANALYTICS_TESTS.md` in the UX-architecture package — still describing the World as optional and the stream axis as a quality ladder three days after D-039 abolished that framing. All six are swept. **The exit evidence is unchanged and is still not met**, because it names an independent reviewer and this was again a producer sweep. Two facts from this round are why that condition exists rather than being pedantry: the widened guard was **silently inert** for its first run through a PowerShell operator-precedence collapse and reported zero hits across 56 files that contained known matches, and the guard's scope still stops at the three packages, so `docs/active/` — which this same slice amended — is unswept. |
| R-035 | The CR-002 amendment changed three separately-accepted packages (D-025 foundation, D-036 UX architecture, D-037 design) and re-froze the D-037 aggregate from `97E79201...120F` to `F16093D0...4AD6`, but no independent design or accessibility review has run against the amended package and the founder has not re-accepted it. | Certain | Medium | accepted_by_founder | Design validator RESULT=PASS 184/0 and production RESULT=PASS 153/0 are producer-run determinism checks, not reviews; `producer-inspection.md` records the amendment as unreviewed | Constitution principle VII forbids the producer approving its own material output, so the passing validators must not be read as acceptance. Recorded at **MA-028**. Until MA-028 is accepted the production contract continues to derive authority from the D-037-accepted package and no production batch may rely on a change introduced only by this amendment. The superseded hashes are retained in `producer-inspection.md`, the production contract, and the design validator comments rather than overwritten, so the amendment is reversible and auditable. Dated review artifacts under `reviews/` and `accessibility/` were deliberately not rewritten. **Founder accepted 2026-09-06 at MA-028 (D-041) without the independent reviews being run.** Acceptance resolves the *authority* question and leaves the *quality* question open: the amended artifacts are accepted, and their accessibility and design correctness is still unreviewed. This risk therefore does not close. Exit evidence remains a clean independent design review and a clean independent accessibility review of the amended package. Any defect they find is a valid finding against an accepted package rather than a reopened decision. Blast radius stays confined to documentation while application and 3D implementation remain blocked. **Updated 2026-09-06 (D-042).** Both reviews have now run and both returned **FAIL**; their blocking findings are remediated and recorded in `reviews/design-review-iteration-4.md` and `accessibility/accessibility-audit-iteration-3.md`. The risk is **not** discharged: the reviews ran against the pre-remediation package and the remediated package has not been re-reviewed. **Exit evidence unchanged** — a clean independent design review and a clean independent accessibility review of the package as it now stands (aggregate `4B9EB9AF...289B`), tracked at **MA-029**. **Updated 2026-09-06 (D-043).** Two independent re-reviews of the D-042 remediation ran and **both returned FAIL again**, converging on five identical defects — most importantly that the stream axis added by D-042 was **inert**: the profiles existed and the validator asserted them by counting rows, but no record selected one, so the very defect the reviews were raised against survived its own fix. That is three FAIL rounds against validator runs of 183, 184, and 185 passing assertions. The D-043 remediation is again **the producer's own work** and again unreviewed. **Exit evidence unchanged** — a clean independent design review and a clean independent accessibility review of the package as it now stands, aggregate `DB92B4D0B889948D6C272F0DC7397327055B0108E7EEB953924F34E735BD4649`, tracked at **MA-029**. Standing lesson recorded in `validation-report.md` and `producer-inspection.md`: a passing validator is a determinism check, not a review, and an assertion that counts rows on a new axis is a placeholder, not coverage. **B01 is held by explicit founder decision until this risk has a clean re-review**, so the blast radius stays confined to documentation and no metered Figma call is spent on frames that would need redrawing. **Updated 2026-09-06 (D-044) — fourth FAIL round.** Both independent re-reviews of the D-043 remediation returned **FAIL**, converging again. The central finding: `stream_profile` was populated as a **total function of `state_profile`**, so the column is derivable and encodes no judgement — no human decided any of its 163 values, all 34 routes take `DS-STREAM-INVARIANT` and structurally cannot do otherwise, and the validator enforces the wrong answer (a producer correctly assigning all four `DS-S-*` to `ROUTE-BOOK` would **fail** `delivery-stream-assignment`). The accessibility reviewer's decisive proof: all 19 route templates depend on `PRIM-001`, the shell hosting the delivery stream control, which the profile's own `exception_rule` forbids — the rule prohibits the assignment the producer made, on every route. Also blocking and found by both: the **production package has no stream axis at all**, so `EC-08`'s evidence-ID grammar cannot name a per-stream frame and `EC-30` would read the second as an illegal overwrite. Four FAIL rounds have now followed validator runs of 183, 184, 185, and 189 passing assertions. **Root cause accepted at D-044:** the producer authored the model, encoded it, wrote the assertion checking it, and certified it. Remediation approach changed accordingly — reviewers now define the model. **Exit evidence unchanged** and still unmet: a clean independent design review and a clean independent accessibility review. **B01 stays held.** **Updated 2026-09-06 (D-044 outcome) — both [PROPOSED] specifications delivered.** `DELIVERY_STREAM_EVIDENCE_MODEL.md` and `accessibility/DELIVERY_STREAM_ACCESSIBILITY_OBLIGATIONS.md` were authored independently, without communication between their authors, and converge on three points (invariance cannot be a producer-asserted claim; the stream control is a radio group with a separate explicit submit; the advisement is persistent visible text). They differ on whether the invariance category is deleted or derived. Neither is accepted; the founder decision is tracked at **MA-030**. This risk does not move on delivery of a specification — it moves only on a clean independent review of an implemented package. **B01 stays held and zero Figma allowance has been consumed.** |
| R-036 | `S-SEMANTIC` is written as a P0 peer stream, but B07 — the only batch carrying any four-stream obligation — is deferred to MA-026, and §0.1 authorizes B01-B06 and B08 only. No four-stream reference evidence is therefore produced under MA-025, so the P0 commitment is written but not discharged on the authorized production path. | Certain | Medium | accepted_by_founder | Accessibility audit iteration 3 finding F-04; `design-batch-plan.csv` B07 `status`; `UI_REFERENCE_PRODUCTION_CONTRACT.md` §2.1 and `SC-10`; founder decision D-042 (d). | Founder chose to leave B07 deferred rather than expand MA-025. Accepted knowingly and recorded rather than treated as satisfied. **Exit evidence:** MA-026 authorizes B07, and the four `DS-S-*` profiles receive their per-stream evidence and independent per-stream accessibility review. Until then no document may state that the four-stream obligation is met. |

| R-037 | Twenty-two judgement columns across the design package are **inert**: each is a total function of a classification the package already makes, so no human decided any of their values and no reviewer can be wrong about them. `time_limit_branch` is among them and founder gate G-5 recorded it as **not exempt**. | Certain | High | awaiting_human | Design validator `A-11` emits one `INERT-REPORT` line per column; `browser_profile` is the single declared exemption, with its reason in the validator source | Do **not** let the producer decide which of the package's own columns carry judgement — that is the exact authorship D-045 bars, and choosing wrongly is how D-042 and D-043 each produced a column that could not be wrong. `A-11` therefore **reports and does not fail**. **Measured 2026-09-06:** of 62 primitives, **50 declare all four streams, 9 declare `STREAM-SCOPE-EXCLUDED`, and only 3 discriminate** — `PRIM-042`, `PRIM-043`, and `PRIM-044`, the World primitives, which correctly withhold `S-SEMANTIC`. MA-030's standing-risk check is therefore **answered in the affirmative**: the column is not constant and no World HUD is claimed present in the semantic stream. But its discriminating power is three rows, and that propagates. A related exposure sits one package over: `stream_disposition` in `batch-production-plan.csv` is constant across all nine batches (`distinct-values=1 rows=9`), a direct consequence of `N-01` placing the semantic host shell in nearly every template's dependencies, and `A-13` likewise reports it rather than failing it. **Exit evidence:** a founder decision, per column, on whether each is a judgement the package must record or a derivation the package should delete, followed by a validator that fails on the ones kept. |
| R-038 | A validator assertion can be **silently inert** — present, named, passing, and testing nothing — and the producer has now shipped three of them in three consecutive remediations. | Certain | High | in_progress | D-042 added `DS-S-*` profiles no record selected; D-043 derived `stream_profile` from `state_profile`; D-045's own peer-framing guard collapsed to a single space-joined literal through PowerShell operator precedence and reported zero hits across 56 files containing known matches | The mechanism in the third case is worth naming because no runtime warning is emitted: **PowerShell's comma operator binds tighter than `+`**, so `@('a' + 'b', 'c' + 'd')` builds **one** `$OFS`-joined element rather than two, and `-join '|'` then has nothing to join. Both validators now assert the alternative count (`degradation-pattern-alternative-count`, `A-16:framing-pattern-is-nine-alternatives`). That is a point fix, not a treatment. The general shape — an assertion whose failure mode is invisible — is undetectable by running the validator, which is the whole difficulty. Partial control: `A-12` and `A-15` carry **negative fixtures** that the guard must reject, so a collapsed or over-narrowed pattern fails rather than passes. **Exit evidence:** every guard in all four validators carries at least one negative fixture, and an independent reviewer confirms each fixture actually exercises the guard it sits beside. |
| R-039 | The design system declares no delivery-stream axis at all — no stream-control component, no four stream tokens, and no 3D focus-ring token — while the stream axis is now normative in the SRS, the style guide, the UX architecture, and both reference packages. | High | Medium | in_progress | `DESIGN_SYSTEM_IMPLICATIONS.md`, one of the thirteen hash-pinned design freeze files, at `0E9FC68C…B763` | Founder gate **G-6** decided to **record the gap now and commission the amendment later as its own bounded specialist contract**, rather than let the producer author design-system content while implementing an evidence model. The file is therefore **untouched and its freeze hash intact**; one edit made during the D-045 slice was reverted and `design-system-freeze-hash` passes. The exposure is that a stream control cannot be drawn in B01 without tokens that do not exist, so this must be resolved **before** the first metered Figma call, not after. **Commissioned at D-046 and accepted at D-047 (both 2026-09-24):** authored as `DESIGN_SYSTEM_AMENDMENT_R-039.md` by a specialist independent of the D-045 producer; independent review 1 returned FAIL on DSA-01 (MEDIUM), resolved in revision 2 (`cf455db`); independent review 2 returned PASS on both parts with DSB-01 and DSB-02 (LOW) carried to application. The freeze file is now being amended by mechanical application under D-047, pending independent verification of the application. **Exit evidence:** an accepted design-system amendment adding the stream axis, the control component, the four stream tokens, and the 3D focus-ring token, produced under its own contract and independently reviewed. |
| R-040 | All 74 baseline frame names were regenerated in one pass under the amended `EC-08` grammar, so a defect in the regeneration affects every frame name simultaneously and no unmigrated row survives as a control. | Medium | Medium | in_progress | D-045 `EC-08` amendment adding a mandatory single-valued `STREAM_<…>` segment; `A-10` resolves every parsed token against the value set the record itself selects | All-or-nothing was chosen deliberately: a partial regeneration leaves a mixed grammar in which `A-10` resolves the old-form names cleanly and fails only the corrected ones, so the migration would punish exactly the rows it fixed. The mitigation is that `A-10` **resolves** rather than pattern-matches — it caught two names that had passed every previous validator, one of which (`STATE_UNAVAILABLE` on `ROUTE-CONTACT`) no reviewer had reported. Residual: no frame has been drawn, so no name has been tested against a real Figma artifact. **Exit evidence:** B01 produces frames whose names match the plan byte for byte, verified against the evidence manifest rather than against the plan alone. |
| R-041 | `A-14`'s per-stream obligation set is entirely **unmet**, and the assertion passes only because every unmet obligation is currently explained by held authorization. If the explanation is ever recorded incorrectly, the assertion becomes permanently satisfiable by omission. | Medium | Medium | in_progress | `evidence-manifest.csv` does not exist; B01 is `future_not_authorized` in both plan files under founder gate G-4 | This is the R-024 lesson applied a second time — never assert that an authorized mutable input stays absent — and it is implemented as a lifecycle condition rather than an unconditional absence check, so the same assertion starts demanding real manifest rows the moment a batch runs. Recorded rather than treated as satisfied. **Exit evidence:** B01 runs, produces a manifest, and `A-14` passes against real per-stream rows rather than against a held-authorization explanation. |

## Review Rule

The coordinating project manager updates probability/impact only when new evidence
exists. A reviewer may close a risk only when its stated exit evidence is durable.
Residual risk acceptance requires an explicit founder decision in
`docs/decisions-log.md` and `DECISIONS.md`.
