# Superseded Iteration-2 Signal Ledger Logo Baseline

**Status:** Superseded by D-027/CR-001; internal rollback and audit evidence only

This directory preserves the exact producer-iteration-2 Signal Ledger logo
geometry and seven logo/mark/optical SVG sources before the final iteration-3
hybrid revision. The founder rejected this logo candidate while retaining the
broader Signal Ledger identity system.

Nothing in this directory is a current identity asset, selectable logo, approved
fallback, production master, public asset, trademark-cleared mark, or source for
new implementation. Current-use guidance and assets live outside `archive/`.

`baseline-inventory.json` records the five CR-001 anchor hashes, each original
source path, the archived path, and the verified SHA-256. A rollback restores the
frozen iteration-2 package only to an unresolved founder gate; it does not approve
the rejected logo.
